#!/bin/bash

for file in *.txt; do
    [[ -f "$file" ]] || continue
    echo "Processing $file"
done

for file in * .*; do
    [[ "$file" == .* ]] && continue
    [[ -f "$file" ]] || continue
    echo "Processing: $file"
done
